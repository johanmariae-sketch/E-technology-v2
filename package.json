{
  "name": "instagram-site-generator",
  "version": "1.0.0",
  "description": "Descarga contenido de Instagram y genera una landing page",
  "private": true,
  "scripts": {
    "download": "python scraper/download.py",
    "process": "npx tsx processor/process.ts",
    "build-site": "cd site && npm run build",
    "dev": "cd site && npm run dev",
    "full-pipeline": "npm run download && npm run process && npm run dev"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.11.0"
  }
}
